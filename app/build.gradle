apply plugin: 'java'

dependencies {
    provided 'com.yuvimasory:orange-extensions:1.3.0'   // OSX support
    compile 'com.formdev:flatlaf:3.4.1'
    compile project(':api')
    runtime project(':services')
}

version = parent.version
